# ── Banco de dados ──
MYSQL_ROOT_PASSWORD=gere_uma_senha_forte_root
MYSQL_PASSWORD=gere_uma_senha_forte_db

# ── Backend ──
# JWT/Session secret (OBRIGATÓRIO em produção — sem ele o backend não sobe)
JWT_SECRET=gere_uma_string_longa_e_aleatoria
NODE_ENV=production

# Origens liberadas no CORS (separadas por vírgula, sem wildcard)
# Ex: https://crm.empresa.com,https://outro.dominio.com
CORS_ORIGIN=http://localhost:9000
